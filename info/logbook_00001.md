# Mittelö Logbook - Session 00001

## Observations
- **Codex Tmux:** The Codex agent session often requires an explicit `Enter` keypress after sending commands via `send-keys`. It seems to buffer input or wait for confirmation.
- **Kiro CLI:** Verified version 1.22.0 at `/Users/tiny/.local/bin/kiro-cli`. Universal binary.
- **Antigravity:** Currently implementing CLI drivers using Gemini 3 Pro model. High latency expected due to comprehensive code generation.

## Actions Pending
- Waiting for Antigravity ACK and code.
- Integration of `backends/*/run` with new `src/wrapper/drivers/*.py`.