# Go port

See `go/` for a minimal client and CLI example that speaks the same JSONL protocol.

Build:

```bash
cd go
go build ./cmd/mitteloctl
```

Notes:
- The module path in `go/go.mod` is a placeholder (`example.com/mittelo`). Change it in your remix if needed.
