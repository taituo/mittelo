{"prompt":"Return exactly: mittelo kiro hardpack ok. Do not use tools. Do not access files. Plain text only."}
{"prompt":"Output ONLY valid JSON (no markdown) for a retry/backoff policy spec with keys: name, goal, constraints (array of strings), parameters (object), algorithm_steps (array of strings), edge_cases (array of strings), metrics (array of strings). Do not use tools or files."}
{"prompt":"Output ONLY a Graphviz DOT graph (no markdown) describing a task lifecycle FSM with states queued, leased, done, failed, and transitions labeled with events (enqueue, lease, ack_done, ack_failed, lease_expired, retry_failed). Do not use tools or files."}
{"prompt":"Compute the worst-case delay until an expired leased task becomes available again if lease_seconds=60 and requeue_expired runs every 10 seconds. Respond with EXACTLY 4 lines: 1) formula, 2) numeric result in seconds, 3) assumption, 4) caveat. No tools/files."}
{"prompt":"Create a Markdown table (only the table) for a system test matrix with columns: backend, prereq, deterministic, expected_status. Include rows for: echo, kiro, codex, claude_code, gemini, kimi, mlx, tmux. deterministic must be yes/no. expected_status must be PASS/SKIP depending on typical local setup. No tools/files."}
{"prompt":"Write 12 acceptance criteria bullets for a backend wrapper contract (stdin->stdout, errors to stderr, exit codes, timeouts, no interactive prompts). Output only bullets (\"- \"). Do not use tools or files."}
{"prompt":"Output ONLY a Python code block (no extra text) with pseudocode for a JSONL-over-TCP client call(method, params) that sends one line and reads one line with a timeout. Do not use tools/files."}
