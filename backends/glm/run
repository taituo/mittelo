#!/usr/bin/env bash
set -euo pipefail

# GLM backend = "Claude Code backend, configured via env exports to use GLM endpoints".
# This wrapper keeps Mittelö's backend contract (stdin -> stdout) and delegates to Claude.

DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$DIR/../claude_code/run"
